
version: 2.1

orbs:
  architect: giantswarm/architect@0.8.0

jobs:
  validate:
    docker:
      - image: quay.io/giantswarm/opsctl
        auth:
          username: $DOCKER_USER
          password: $DOCKER_PASS
    steps:
      - checkout
      - run:
          name: Validate AWS releases
          command: opsctl validate releases -p aws.yaml
      - run:
          name: Validate Azure releases
          command: opsctl validate releases -p azure.yaml
  prepare-helm:
    docker:
      - image: bash:5.0.16
    parameters:
      app_name:
        description: "Name of helm chart."
        type: "string"
      source:
        description: "Directory to deploy as helm chart."
        type: "string"
    steps:
      - checkout
      - run: mkdir -p helm/<<parameters.app_name>>
      - run: mv helm/templates helm/<<parameters.app_name>>/templates
      - run: cp <<parameters.source>>.yaml helm/<<parameters.app_name>>/values.yaml
      - run: |
          cat \<<EOF > helm/<<parameters.app_name>>/Chart.yaml
          name: <<parameters.app_name>>
          version: [[ .Version ]]
          EOF
      - persist_to_workspace:
          root: .
          paths:
            - ./helm

workflows:
  workflow:
    jobs:
      - validate
      - prepare-helm:
          name: prepare-helm-kvm
          app_name: releases-kvm
          requires:
            - validate
          source: kvm
      - architect/push-to-app-catalog:
          name: push-releases-kvm-to-control-plane-app-catalog
          app_catalog: control-plane-catalog
          app_catalog_test: control-plane-test-catalog
          attach_workspace: true
          chart: releases-kvm
          requires:
            - prepare-helm-kvm
      - architect/push-to-app-collection:
          name: push-releases-to-kvm-app-collection
          app_name: releases-kvm
          app_collection_repo: kvm-app-collection
          unique: "true"
          requires:
            - push-releases-kvm-to-control-plane-app-catalog
          filters:
            # Trigger the job on merge to master.
            branches:
              only: master
